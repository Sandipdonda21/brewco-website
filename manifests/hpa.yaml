apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: test-demo
  labels:
    app.kubernetes.io/name: test-demo
    app.kubernetes.io/part-of: test-demo-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-demo
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80